import styled from 'styled-components'
import { grey } from "@material-ui/core/colors";

export const S = {}
S.Box = styled('div')`
  display: flex;
  align-items: center;
  background: ${grey[700]};
  
`

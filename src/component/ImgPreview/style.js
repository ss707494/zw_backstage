import styled from "styled-components"
import { Dialog } from "@material-ui/core";

export const S = {}
S.Box = styled(Dialog)`
  
`

S.Img = styled('section')`
  > img {
    max-width: 80vw;
    max-height: 60vh;
  }
`
